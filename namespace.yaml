apiVersion: v1
kind: Namespace
metadata:
  name: roboshop   # Change this to your desired namespace name
  labels:
    name: roboshop     # Optional: label for identification
    environment: dev   # Optional: label for categorization
    purpose: poc      # Optional`: label for purpose